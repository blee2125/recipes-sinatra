<h2>Every Recipes</h2>

<% if logged_in? %>
    <% @recipes.each do |recipe| %>
        <a href="/recipes/<%= recipe.slug %>"><%= recipe.name %></a><br>
    <% end %>
<% else %>
    <p>To view recipes, <a href="/login">login</a> or <a href="/signup">signup</a>.</p>
<% end %>